Bootstrap: docker
From: debian:latest

%files
  . /project

%post
  apt update
  apt install -y build-essential cmake gcc openmpi-bin libopenmpi-dev
  
  cd /project
  mkdir -p build && cd build
  cmake ..
  cmake --build .
  cp /project/build/main /project