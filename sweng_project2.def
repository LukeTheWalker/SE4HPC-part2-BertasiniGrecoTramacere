Bootstrap: docker
From: mfisherman/openmpi

%files
  . /project

%post
  apk update
  apk install -y build-essential cmake gcc 
  
  cd /project
  mkdir -p build && cd build
  cmake ..
  cmake --build .
  cp /project/build/main /project